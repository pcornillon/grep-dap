# study_phase — learn OPeNDAP and pydap

The first investigation, 2026-05-16, and the shortest. Objective, from
`prompts/study_phase.md`: read the OPeNDAP and pydap documentation and write it up,
focusing on **metadata, data retrieval and subsetting**. It is the groundwork the URI
test case then stood on — `prompts/test_case_uri.md` hands the resulting reference
document forward as context.

## The scripts this issue uses

**None.** This phase read documentation; it did not probe a server. The probe scripts
in `../Python/` all belong to `../uri_test_case/`.

## What is here

| File | What it is |
|---|---|
| `study_phase_log.tex` | the working log — commands run and reasoning |

## What it produced, and where it went

**`../../DOCS/opendap_readme.tex`** — the OPeNDAP + pydap reference. It lives in
`DOCS/` rather than here because it is standing reference that later work reads, not a
record of this investigation.
