# Session — 2026-08-04 16:21 EDT · satdat1
<!-- session: eb120b6e-6c4a-41c5-aeb9-7cedbde7016e -->

**Lane:** whole repo (no concurrent session known).

## P1 · 2026-08-04 16:21 EDT · what "origin/uri is 1 ahead, 3 behind" means

Question, not work. Peter asked what the divergence report from the restructure
session meant, and said he wanted to work in `uri`.

**Read:** `git branch -avv`, `git log --graph --all`, `git show --stat c260ef7`,
`git diff 07c10f9 c260ef7 -- prompts/`, `git diff c260ef7 main -- prompts/`,
`git ls-tree main`.

**Found:**

- The branches diverged at `07c10f9` (Xavier's PR merge, 2026-04-15). `origin/uri`
  carries one commit past it (`c260ef7`, 2026-04-28); `origin/main` carried three
  (2026-05-17, 2026-05-18, 2026-08-04 15:21) at the time the count was taken, and
  four now.
- `c260ef7` is the *only* commit ever made on `uri`: it repointed the two prompt
  files at `/Users/petercornillon/Git_Repos/grep-dap/MD/`, deleted twelve of
  Xavier's `scripts/*.py`, added `MD/.gitkeep`, and commented out prompts 2–6.
- Every one of those changes was superseded on `main`, independently: `main`'s
  prompt files point at `ISSUE_ANALYSES/` and `DOCS/`, carry the full eight-prompt
  sequence, and `main` has no `scripts/` and no `MD/`.
- All of the actual proof-of-concept output — `uri_test_case_*.tex`, logs 1–7, the
  two curator reports, `usage_sst.md`, `usage_gradient_SST.md`, 32 Python probes —
  exists only on `main`.

**Conclusion reported:** the premise is wrong. `main` is the branch with the work;
`uri` is a three-month-old dead end holding nothing unique. Recommended deleting
`uri` rather than merging or switching to it.

**Not done:** nothing deleted or pushed — awaiting Peter's answer.

**Noticed in passing:** `LOG.md`'s restructure entry cites session key
`1527_satdat1`, but `SESSIONS/` holds no such log. Flagged, not fixed. *(Resolved in
P2 — the log existed, in `claude-config`.)*

## P2 · 2026-08-04 16:24 EDT · execute the three asks

Peter: yes to deleting `uri`, yes to pushing `main`, reconstruct the missing log.

**Pushed** `main` — `60ef4fd..a55f97b`. Local and `origin/main` now agree.

**Deleted** `origin/uri` — `git push origin --delete uri`, tip
`c260ef7cab953fe2fb31a7912cf3c948b3935aef` recorded first. `main` is now the only
branch. Written up as **D6** and as the resolution of Task #1.

**The "missing" session log was not missing.** Before reconstructing anything I looked
for the source, and found the restructure session's log complete and detailed at
`~/Git_Repos/claude-config/SESSIONS/2026-08-04_1527_EDT_satdat1.md` — that session's
working directory was `claude-config`, so its log went there and grep-dap got none,
while grep-dap's `LOG.md` cited the key. Found via the Stop-hook transcript
`~/Dropbox/claude/transcripts/ef702d45-….md`, matched by session id.

So this is an **extraction, not a reconstruction**: a grep-dap-scoped log written from
that source, which is cited in its header and remains the fuller cross-repo record.

**Re-verified the source rather than copying it, and three claims failed.** All three
concern `c260ef7` and all three had propagated into `TASKS.md` #1 and `STATUS.md`:

| Claim | Check | Result |
|---|---|---|
| deletes "all 14 `.py` scripts" | `git ls-tree -r 07c10f9 -- scripts/` | **12**, not 14 — "all" was right, the count was not |
| adds `gradients_by_period_umm_c.json` | `git show --name-status c260ef7` | **never touched it** — present at the merge base; `main` deleted it in `9776b97` |
| the JSON + prompt edits are "the only content `main` does not have" | file-by-file diff | **nothing was unique** — `main` re-did the prompt localization independently and further |

The second one had consequences: the JSON was the sole content-bearing item behind
Task #1's recommended option 2, "cherry-pick then abandon." There was nothing to
cherry-pick, so option 1 was correct on the merits and not merely on Peter's
preference.

**Edited:** `SESSIONS/2026-08-04_1527_EDT_satdat1.md` (new), `TASKS.md` #1 → `done`,
`STATUS.md` (Waiting-on-Peter, active thread, timestamp), `DECISIONS.md` D6, `LOG.md`.

**D6 confirmed by Peter** at 16:41 EDT, wording as drafted, including the live tension
— the branch's original purpose is not recoverable and he did not supply it.

**Committed and pushed** on his word.

**Follow-on, in `claude-config`:** he also approved a standing rule for the cross-repo
log split found above — a session log belongs to the repository being worked on, not
to the session's working directory. Written up there, not here.
